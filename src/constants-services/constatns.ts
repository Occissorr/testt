//#region ENUMs
export enum Layouts {
    Horizontal = 'horizontal',
    Vertical = 'vertical'
};

export enum Positions {
    Top = 'horizontal',
    Bottom = 'vertical',
    Left = 'left',
    Right = 'right'
}

export enum Shapes {
    Square = 'square',
    Circle = 'circle'
}